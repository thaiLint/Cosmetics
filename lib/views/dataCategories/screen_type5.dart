import 'package:flutter/material.dart';

class ScreenType5 extends StatelessWidget {
  const ScreenType5({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color.fromARGB(255, 243, 121, 33),
        title: Text("Miking"),
      ),
    );
  }
}
